#sudo: required

language: node

services:
  - docker

before_install:
- docker pull cypress/internal
- docker run cypress/internal /bin/sh -c "cypress run"
#
#script:
#  - npm install
#  - node_modules/.bin/cypress run