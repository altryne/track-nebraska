#sudo: required

language: node

services:
  - docker

before_install:
- docker pull cypress/internal:6
- docker run cypress/internal:6 /bin/sh -c "cypress run"
#
#script:
#  - npm install
#  - node_modules/.bin/cypress run