#sudo: required

language: node

services:
  - docker

before_install:
- docker pull cypress/internal:cy
- docker run cypress/internal:cy /bin/sh -c "cypress run"
#
#script:
#  - npm install
#  - node_modules/.bin/cypress run